@inherits MemeRepoBaseFluxorComponent

<div class="folder-page justify-space-between">
    <MemeRepoStateContainer State="@state">

        <LoadingFragment>
            <MemeRepoLoading />
        </LoadingFragment>

        <ContentFragment>

            <MudBreadcrumbs Items="@folderBreadcrumbItems" Class="folder-breadcrumbs">
                <ItemTemplate Context="item">
                    <MudText Class="custom-breadcrumb-item" @onclick="_ => OnFolderBreadcrumbItemClick(item.Href)">@item.Text</MudText>
                </ItemTemplate>
            </MudBreadcrumbs>
            <ContextMenuTrigger MenuId="@(ContextMemuAlias.FolderPage)" CssClass="w-100 h-100" Data="@(currentFolderId)">
                <MemeRepoContainer Items="folderObjects">
                    <EmptyContent>
                        <div class="folder-empty-message h-100">
                            <div class="d-flex flex-row align-center">
                                <MudIcon Icon="@Icons.Filled.FolderOpen" Style="height: 70px; width: 70px;" />
                                <MudText Style="font-size: 50px"> Мемчиков нема</MudText>
                            </div>
                            <MudText Class="mt-0">Наполни меня (͡° ͜ʖ ͡°)</MudText>
                        </div>
                    </EmptyContent>
                </MemeRepoContainer>
            </ContextMenuTrigger>

        </ContentFragment>

        <ErrorFragment>

        </ErrorFragment>

    </MemeRepoStateContainer>
</div>
