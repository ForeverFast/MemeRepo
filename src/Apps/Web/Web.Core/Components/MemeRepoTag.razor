@inherits MemeRepoBaseComponent

<div class="memerepo-tag  @Classname" style="@Stylename" @attributes="UserAttributes" @onclick="OnClick">
    <ContextMenuTrigger MenuId="@(ContextMemuAlias.Tag)" CssClass="context-menu-trigger-block d-flex flex-column align-center" Data="@Data.Id">
        <div class="inner-container border-2 rounded-xl px-1 d-flex flex-row align-center">
            <MudIcon Icon="@Icons.Filled.Tag" />
            <p class="marquee"><span>@Data.Title</span></p>
        </div>
    </ContextMenuTrigger>
</div>